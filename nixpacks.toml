[phases.setup]
nixPkgs = ['python311', 'gcc', 'zlib', 'libjpeg', 'freetype', 'lcms2', 'openjpeg', 'libtiff', 'libwebp']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --threads 8'
```

Create `.python-version` in `yolo-backend/`:
```
3.11.0