[phases.setup]
nixPkgs = ['python313', 'gcc', 'zlib', 'libjpeg', 'freetype', 'lcms2', 'openjpeg', 'libtiff', 'libwebp']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --threads 8'
```


Create `aptfile` in `yolo-backend/`:
```
python3-dev
build-essential
libjpeg-dev
zlib1g-dev
libfreetype6-dev
liblcms2-dev
libopenjp2-7-dev
libtiff-dev
libwebp-dev